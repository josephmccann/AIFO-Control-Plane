# Remote State Bootstrap

Creates the S3 bucket used by Terraform remote state.

This root intentionally does not configure a remote backend for itself. For the first bootstrap, run it with local state under controlled conditions, then preserve or migrate that bootstrap state according to the platform runbook.

No resources have been created from this repository yet.
